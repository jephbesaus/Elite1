# Copiez ce fichier en .env.local pour le développement local (npm run vercel dev),
# et ajoutez la même variable dans Vercel → Project Settings → Environment Variables
# pour la production.

ANTHROPIC_API_KEY=sk-ant-votre-cle-ici
